def configure_collab_gpu()
    '''
    This function executes commands in the jupyter terminal such that the environment is configured to run gpu-enabled 
    libraries when it is possible (for example, to set light-gbm to run in gpu environments) 
    '''